
name: 'solodshouse'
version: '0.1.0'
config-version: 2

profile: 'solodshouse'

model-paths: ["models"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  solodshouse:
    staging:
      +materialized: view
      +schema: staging
