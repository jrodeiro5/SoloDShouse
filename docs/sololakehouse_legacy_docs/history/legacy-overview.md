# Legacy Runtime Overview

This document archives retired runtime paths and where their history is preserved.

## Retired from active codebase

- Host-side linear script orchestration (`scripts/run-pipeline.py`)
- Make targets and flags for parallel runtime switching (`pipeline-v1`, `pipeline-legacy`, `PIPELINE_MODE`)
- Optional-profile posture for OpenMetadata and Superset

## Current policy

- Active runtime guidance appears only in main docs and reflects v2.5 single-track execution.
- Legacy content remains reference-only in `docs/history/`.

## Legacy references

- `docs/history/timeline.md`
- `docs/history/architecture-evolution.md`
- `docs/history/v2-planning.md`
